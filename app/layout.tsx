import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'A Nova Moeda: Tempo e Atenção | Mini-Teaching Marketing Digital',
  description: 'Descubra como a economia da atenção está revolucionando o marketing digital. Aprenda estratégias para capturar e manter a atenção do seu público em um mundo de distrações.',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
